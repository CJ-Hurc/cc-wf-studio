{
  "schema": "hurc-complete-e2e-runtime/v1",
  "description": "Runtime adapter: emit KIND_PACKS-registered cli + nested packages/vscode contributes (no unknown-surface-kind capability:* stubs).",
  "adapters": [
    {
      "id": "cc-wf-studio-consumer-cli",
      "kind": "cli",
      "surface_kind": "cli",
      "timeout_sec": 300,
      "argv": [
        "python3",
        "scripts/complete-e2e/list-surfaces.py"
      ]
    }
  ]
}
